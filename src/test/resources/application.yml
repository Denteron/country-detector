spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    password: root
    username: root
    url: jdbc:h2:mem:testDB
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: db/changelog/liquibase-migration-1.xml
